import { createApp } from 'vue';
import App from './App.vue';
import router from './router';


// Create the first Vue application
createApp(App)
  .use(router)
  .mount('#app');

// Create the second Vue application

